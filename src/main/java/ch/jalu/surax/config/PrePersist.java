package ch.jalu.surax.config;

/**
 * Interface for classes that perform an action before data is persisted.
 */
public interface PrePersist {

    // TODO: Revise this architecture
    void prePersist();

}
